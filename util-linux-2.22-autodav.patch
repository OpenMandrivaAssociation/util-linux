--- util-linux-2.22-rc2/libmount/src/context.c~	2012-07-23 23:21:56.316319801 +0200
+++ util-linux-2.22-rc2/libmount/src/context.c	2012-08-20 14:10:26.839999632 +0200
@@ -1417,6 +1417,8 @@ int mnt_context_guess_fstype(struct libm
 				free(type);	/* type is not cached */
 		}
 	} else {
+		if (!strncasecmp(dev, "http://", 7) || !strncasecmp(dev, "https://", 8))
+			rc = mnt_fs_set_fstype(cxt->fs, "davfs");
 		if (strchr(dev, ':') != NULL)
 			rc = mnt_fs_set_fstype(cxt->fs, "nfs");
 		else if (!strncmp(dev, "//", 2))
